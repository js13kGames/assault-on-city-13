export const ft = (fontSize: number) => {
  return `${fontSize}px 'Consolas', 'Menlo', 'DejaVu Sans Mono', monospace`
  // return `${fontSize}px 'Courier New', Courier, 'Lucida Console', Monaco, monospace`;
  // return `${fontSize}px system-ui`;
}
export const ftb = (fontSize: number) => {
  return `bold ${fontSize}px 'Consolas', 'Menlo', 'DejaVu Sans Mono', monospace`
  // return `bold ${fontSize}px system-ui`;
}
